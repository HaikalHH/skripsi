# Shared Package

Kontrak yang dipakai lintas app.

- `schemas/`: Zod schema untuk payload dan kontrak API internal.
- `types/`: tipe TypeScript yang dipakai beberapa package.
- `prompts/`: prompt builder yang dipakai service AI.
- `utils/`: helper kecil lintas package.
